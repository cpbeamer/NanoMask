apiVersion: v1
kind: Service
metadata:
  name: {{ include "nanomask.fullname" . }}
  labels:
    {{- include "nanomask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nanomask.selectorLabels" . | nindent 4 }}
